{
  "name": "code-cleaner-by-siddarthsbalaji",
  "displayName": "CodeCleaner",
  "description": "An intelligent, zero-configuration, language-aware extension to clean, compact, and minify your source code directly inside VS Code. ",
  "version": "0.0.2",
  "publisher": "SiddarthSBalaji",
  "engines": {
    "vscode": "^1.120.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:code-cleaner.run",
    "onCommand:code-cleaner.cleanFolder"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "code-cleaner.run",
        "title": "Clean Code Engine"
      },
      {
        "command": "code-cleaner.cleanFolder",
        "title": "Clean Folder"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "code-cleaner.run",
          "group": "modification"
        }
      ],
      "explorer/context": [
        {
          "command": "code-cleaner.cleanFolder",
          "group": "modification",
          "when": "explorerResourceIsFolder"
        }
      ]
    },
    "configuration": {
      "title": "Code Cleaner",
      "properties": {
        "code-cleaner.removeEmojis": {
          "type": "boolean",
          "default": true,
          "description": "Remove emojis when cleaning code."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.120.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.9.1",
    "eslint": "^9.39.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  }
}